# Partner API - centralized config (overrides shared defaults)
issuers: ${COGNITO_ISSUER_URL:https://cognito-idp.us-east-1.amazonaws.com/us-east-1_4atAJ9eRI}
issuers-keys: ${COGNITO_ISSUER_URL:https://cognito-idp.us-east-1.amazonaws.com/us-east-1_4atAJ9eRI}/.well-known/jwks.json
audiences: ${COGNITO_AUDIENCE:20kkrgk5cghmtoc31jsq8mmshi,3qj47e7b7cd6augv7ginnqef8,4d38h6sca3bnn33fm89pb38kt2,3q49aa6mhrnjhmhcm7ta9uj28k}

server:
  port: 9012

kafka:
  consumerGroupId: ${KAFKA_CONSUMER_GROUP:velaris-partner}
  legacyReservationsTopic: ${KAFKA_RESERVATIONS_TOPIC:velaris-reservations}
  legacyClaimsTopic: ${KAFKA_CLAIMS_TOPIC:velaris-claims}
  propertyPoliciesTopic: ${KAFKA_POLICIES_TOPIC:velaris-property-policies}

hubspot:
  token: ${HUBSPOT_TOKEN:}

lodgify:
  host: ${LODGIFY_HOST:damage-protection.lodgifyintegration.com}
  apiKey: ${LODGIFY_API_KEY:}

stripe:
  sk: ${STRIPE_SECRET_KEY:}
  pk: ${STRIPE_PUBLISHABLE_KEY:}
  identity:
    webhookSecret: ${STRIPE_IDENTITY_WEBHOOK_SECRET:}

velaris:
  legacy:
    baseUrl: ${VELARIS_LEGACY_BASEURL:https://legacy-api.test.velaris.com}
    access_token: ${VELARIS_LEGACY_ACCESS_TOKEN:}
    access_username: ${VELARIS_LEGACY_ACCESS_USERNAME:partner.api@velaris.com}
    access_password: ${VELARIS_LEGACY_ACCESS_PASSWORD:}
  claimLinkTemplate: ${VELARIS_CLAIM_LINK_TEMPLATE:https://claim.test.velaris.com/?claim_token=%s}

management:
  server:
    port: 8093
